---
# Logrotate tasks for Proxmox Backup role

- name: Deploy logrotate configuration
  ansible.builtin.template:
    src: logrotate-pve-backup.j2
    dest: /etc/logrotate.d/pve-backup
    mode: "0644"
    owner: root
    group: root
