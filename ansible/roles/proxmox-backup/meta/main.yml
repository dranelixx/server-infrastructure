---
# Meta information for Proxmox Backup role

galaxy_info:
  author: akonopcz
  description: Backup Proxmox VE configuration to remote storage via rsync/SSH
  license: MIT
  min_ansible_version: "2.14"

  platforms:
    - name: Debian
      versions:
        - bullseye
        - bookworm

  galaxy_tags:
    - proxmox
    - backup
    - rsync

dependencies: []

# Required collections (install with: ansible-galaxy collection install community.crypto)
# - community.crypto (for openssh_keypair module)
